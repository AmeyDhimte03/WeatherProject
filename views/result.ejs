<%- include('partials/header'); -%>

    <div class="center">

        <h1 class="heading" id="heading">
            <% if (code===200) { %>
                Today's weather in
                <%=query %>
                    could be described as
                    <%=weatherDes %>
                        <br> with tempearture of
                        <%=temp %>
                            degrees celcius & humidity as
                            <%=humidity %>
        </h1>
        <img src="<%= imgURL %>" alt="weatherDes" class="">
        <% } else { %>
            <h1 class="heading">
                <%= message %>
            </h1>
            <% } %>
    </div>
    <form action="/" method="get"><button type="submit" class="hovering-button">🔙</button></form>
    <%- include('partials/footer'); -%>